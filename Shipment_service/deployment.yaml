apiVersion: apps/v1
kind: Deployment

metadata:
  name: shipment-service
  namespace: shipops

spec:
  replicas: 1

  selector:
    matchLabels:
      app: shipment-service


  template:

    metadata:
      labels:
        app: shipment-service


    spec:

      containers:

      - name: shipment-service

        image: 760402325804.dkr.ecr.us-east-1.amazonaws.com/shipops/shipment:latest

        imagePullPolicy: IfNotPresent


        ports:

        - containerPort: 5000


        env:

        - name: PORT
          value: "5000"


        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: shipment-service-secret
              key: DATABASE_URL
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: auth-service-secret
              key: JWT_SECRET
        - name: REFRESH_TOKEN_SECRET
          valueFrom:
            secretKeyRef:
              name: auth-service-secret
              key: REFRESH_TOKEN_SECRET
        - name: CORE_SERVICE_URL
          value: "http://core-service:5002/api/v1"
