apiVersion: v1
kind: Secret
metadata:
  name: ai-service-secret
  namespace: shipops
type: Opaque
stringData:
  # Replace this placeholder before applying the manifest. Do not commit a real key.

  #remove the comment from the next line 
  OPENROUTER_API_KEY: ""
